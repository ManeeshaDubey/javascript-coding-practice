# JavaScript Coding Practice

A collection of JavaScript coding problems to practice from basic to advanced level.

---

## ✅ Basic JavaScript Coding Problems

- Write a function to add two numbers.
- Write a function to find the maximum number in an array.
- Check if a string is a palindrome.
- Reverse a string or an array.
- Find the factorial of a number.
- Generate the Fibonacci sequence up to N terms.
- Count vowels and consonants in a string.
- Find if a number is prime.
- Find the sum of digits of a number.
- Convert a string to title case.

---

## ✅ Intermediate JavaScript Coding Problems

- Find the second largest number in an array.
- Remove duplicates from an array.
- Merge two arrays and sort them.
- Find the intersection and union of two arrays.
- Flatten a nested array.
- Implement a function to debounce another function.
- Implement a function to throttle another function.
- Find whether two strings are anagrams.
- Count the frequency of each element in an array.
- Find the longest word in a string.

---

## ✅ Object and Array Manipulation

- Add and delete properties from an object.
- Deep clone an object.
- Sort an array of objects by a specific key.
- Group objects by a key.
- Convert an object to an array of key–value pairs.
- Create an array of unique objects based on a property.
- Merge two objects.
- Check if two objects are deeply equal.
- Retrieve nested object values safely.
- Swap keys and values in an object.

---

## ✅ String and Regex Challenges

- Remove whitespace from a string.
- Replace all instances of a character in a string.
- Count how many times each word appears in a sentence.
- Find common characters in two strings.
- Extract numbers from a string.
- Check if a string contains only unique characters.
- Reverse the order of words in a sentence.
- Truncate a string to a specific length.
- Convert `snake_case` to `camelCase`.
- Validate an email format using regex.

---

## ✅ Advanced / Logical Challenges

- Implement `Promise.all()` manually.
- Implement your own `map()`, `filter()`, and `reduce()`.
- Create a custom event emitter (`on`, `off`, `emit`).
- Implement a memoization function.
- Write a deep equality checker.
- Simulate `setTimeout()` using Promises.
- Write a function that delays execution using `async/await`.
- Implement a retry mechanism for failed Promises.
- Write a function to flatten deep nested objects.
- Implement your own `bind()`, `call()`, and `apply()`.

---

## 🎯 Bonus / Tricky JavaScript Challenges

- Explain what happens in `console.log(1 + '2' + 3)`.
- Implement a polyfill for `Promise.resolve()`.
- Implement an LRU Cache.
- Write a function to find pairs that sum to a target.
- Find missing numbers in a sequence.
- Implement a binary search algorithm.
- Generate all permutations of a string.
- Find the longest substring without repeating characters.
- Check balance of parentheses in a string.
- Implement a simple event loop simulation.

---

### ⭐ Happy Coding! 🚀  
Feel free to contribute or practice solving these problems.
